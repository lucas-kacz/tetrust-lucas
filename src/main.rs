extern crate piston_window;
extern crate rand;

use piston_window::*;

use rand::seq::SliceRandom;
use rand::thread_rng;

use std::io::BufReader;
use std::fs::File;



fn main() {
    println!("Hello, world!");
}
